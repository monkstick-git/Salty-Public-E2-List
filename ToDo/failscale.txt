@name FAILSCALE
@inputs 
@outputs 
@persist Prop:array Props:entity A B:array
@trigger 
runOnTick(1)
if(first()|duped()){
findIncludePlayerProps(owner())
findByClass("prop_*")
Prop = findToArray()
}
if(tickClk()){
#A=A+1
for(I=1,Prop:count()){

    Props = Prop[I,entity]
B:add(Props():pos())
}
#vec(A*10,A*10,A*10)
}
