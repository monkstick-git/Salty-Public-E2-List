@name SampleE2
@inputs 
@outputs 
@persist
@trigger 
interval(100)
if(first()){
for(I=1,150){
 holoCreate(I)
holoScale(I,vec(1,1,1))
holoModel(I,"hq_sphere")
}
}
while(perf()){
for(I=1,150){
 holoPos(I,entity():pos() + vec(sin(I*10)*1000,cos(I*curtime())*500,sin(I*10*curtime()*100)*500))
}
}
