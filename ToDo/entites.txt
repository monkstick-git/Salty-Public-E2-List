@name entites
@inputs 
@outputs 
@persist Timer
@trigger 
interval(1)
findByClass("prop_*")
findSortByDistance(entity():pos())
Target = find()
Timer = Timer + 1
if(Target:pos():distance(entity():pos()) < 500){
Target:setPos(entity():pos() + vec(sin(Timer)*300,cos(Timer)*300,20))
}
