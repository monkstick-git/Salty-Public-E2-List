@name setangengine
@inputs Wheel1:entity W Body:entity
@outputs 
@persist Power
@trigger 
interval(10)
if(W)
{
 Power = clamp(Power + 0.5,0,50) 
}else{
Power = clamp(Power - 1,0,50)
}

entity():isWeldedTo():setAng(Body:angles())

#Wheel1:setAng(Wheel1:toWorld(ang(0,-Power,0)))
Wheel1:setAng(Wheel1:toWorld(ang(0,-Power,0)))
