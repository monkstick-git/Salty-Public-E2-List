@name TextGen v1
@inputs 
@outputs 
@persist Int
@trigger 
interval(10)

Int = Int + 1
Str = abs(floor(cos(Int) * 10)):toString()
Array = Str:explode("")

for(I=1,Array:count()) {
    Letter = "models/sprops/misc/alphanum/alphanum_" + Array[I,string] +".mdl"
    if(Array[I,string] != " "){
    holoCreate(I)
    holoModel(I,Letter)
    holoPos(I,entity():pos() + vec(I * -10,0,30))
}
}
